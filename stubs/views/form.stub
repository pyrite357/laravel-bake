<form method="POST" action="{{ $action ?? '' }}">
    @csrf
    @method($method ?? 'POST')

    {{ form_fields }}

    <button type="submit" class="btn btn-primary">{{ $buttonText ?? 'Save' }}</button>
</form>
